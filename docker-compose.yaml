# PostgreSQL service removed in Phase 3 (local-first SQLite architecture).
# All data now lives under ~/.tmpo/ (tmpo.db, config.toml, runs/).
# This file is kept for reference; no services are needed to run tmpo locally.
